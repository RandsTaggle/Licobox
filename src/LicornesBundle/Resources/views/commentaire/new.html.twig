{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nouveau commentaire</h1>

    {{ form_start(form) }}
        {{ form_widget(form.licorne) }}
        {{ form_widget(form.contenu) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('commentaire_index') }}">Retour a la liste</a>
        </li>
    </ul>
{% endblock %}
